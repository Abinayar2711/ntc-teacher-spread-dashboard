[theme]
# The chart palette is validated against the light chart surface, so the app is
# pinned to light rather than following the viewer's OS theme.
base = "light"
backgroundColor = "#fcfcfb"
secondaryBackgroundColor = "#f0efec"
textColor = "#0b0b0b"
